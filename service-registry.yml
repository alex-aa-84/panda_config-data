#Application Configuration
eureka:
  instance:
    hostname: service-registry.azurewebsites.net
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl: 
      defaulZone: https://${eureka.instance.hostname}/eureka/